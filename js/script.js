// Selects the top button
const topButton = document.querySelector(".top-button");

// Scrolls window to top if top button is clicked
topButton.addEventListener("click", () => {
  window.scrollTo(0, 0);
})
